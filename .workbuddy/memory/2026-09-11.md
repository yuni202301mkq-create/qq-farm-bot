# 2026-09-11 工作日志

## 抓包代理端口随机化实现

将手机 Wi-Fi 代理端口从固定 18000 改为 18000-18999 范围随机选用：

- `core/src/capture/config.js`: parsePortRange 不再塌缩范围；normalizeConfig 保留 proxyPortTo 差异（< from 时回退）；DEFAULT_CONFIG 改为 proxyPortTo: 18999
- `core/src/capture/mitm-proxy.js`: listenOnConfiguredPort 从 from~to 范围随机起始偏移，顺序尝试空闲端口，全部占用则报错
- `core/data/capture/config.json`: proxyPortTo 改为 18999
- `core/src/capture/api-server.js`: health portPool 改为 [from, to]
- `docker-compose.yml`: 端口映射改为 18000-18999:18000-18999
- `web/src/components/admin/AdminSystemPanel.vue`: 文案改为"代理端口范围 18000–18999（启动时随机选用）"
- `core/test/capture-config.test.js`: 更新断言匹配新行为（范围解析、normalizeConfig 保留 to、to<from 回退）

测试：vue-tsc + vite build 通过。

## 宠物日记活动面板：钱包弹窗移动端左侧裁切修复

`web/src/components/activity/PetDiaryActivityPanel.vue`：

- `.pet-wallet` max-width 改为 `min(80cqw, calc(100cqw - 8px))`，确保不超出容器查询宽度
- `.pet-wallet` 添加 `z-index: 6` 确保弹窗在顶层
- 520px 断点：wallet 面板 `right: -6px; width: min(300px, calc(100cqw - 16px)); max-width: none;`，避免左侧溢出被 `.pet-scroll` 的 `overflow: hidden` 裁切

测试：vue-tsc + vite build 通过。
